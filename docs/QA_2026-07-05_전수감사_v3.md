# QA 전수 감사 v3 (2026-07-05)

- 범위: 전 페이지(~260 라우트) + API/크론(~60) + firestore.rules/indexes. 7개 영역 병렬 감사 후 상위 발견 직접 재검증.
- 중점 렌즈: QA v2 이후 변경분 — 탭 개편·주제 탐색, /research 팔레트, 스튜디오 템플릿, 보상 원장(mirror), C-1~C-5, 신규 크론.
- ✔ = 오케스트레이터가 코드/운영에서 직접 확정. (PLAUSIBLE) = 재현 조건 미확정.

## 즉시 조치 완료 (감사 중 핫픽스 배포됨)

- ✔ **알림 정리 크론이 신규 알림까지 삭제** — `api/cron/notifications-cleanup/route.ts`. createdAt 타입 혼재(클라이언트 dataApi.create=Timestamp, 서버 fan-out=ISO 문자열) 때문에 Timestamp형 알림이 나이 판정을 오통과해 매일 삭제됨. 메모리 필터 ISO 정규화로 수정, 커밋·배포 완료.

## Critical

1. ✔ **연구활동 리포트 인쇄가 빈 문서 출력** — `src/components/mypage/MyResearchView.tsx:361` + `src/app/globals.css:196-200`. Tabs 루트의 `print-hide`(조상)가 내부 `ResearchReportPrint`(.report-print)까지 숨김 — 자손 `display:block!important`로 복구 불가. "연구활동 리포트 출력→인쇄" 시 빈 PDF. (개편 전부터 존재한 라이브 결함)
2. ✔ **졸업생 논문 목록, 비로그인 접속 시 페이지 크래시** — `src/app/alumni/thesis/page.tsx:90`. 비로그인 게이트 early return이 훅들 사이에 위치 → initialized 전환 리렌더에서 훅 개수 감소("Rendered fewer hooks") 크래시. 게이트가 보호할 대상이 에러 화면을 봄.
3. ✔ **협업연구 팀 생성 전면 실패** — `src/lib/bkend.ts:3069-3076` vs `firestore.rules:1479-1484`. 리더 본인 멤버 등록이 `userId==invitedBy==본인`인데 룰 두 분기(타인 등록 / invitedBy≠본인) 모두 불통 → permission-denied + 고아 연구 문서 잔존.
4. **협업연구 챕터 댓글이 누구에게도 로드 안 됨** — `bkend.ts:3374-3383` vs `rules:1552-1557`. `listByChapter`가 chapterId만 필터 — researchId 기반 룰을 쿼리로 증명 불가 → 전원 list 거부(작성은 되는데 목록 항상 빈 상태). 수정: 쿼리에 `where("researchId","==",…)` 추가.
5. ✔ **복합 인덱스 드리프트 — 공개 캘린더 피드 현재 500 (라이브 확인)** — `firestore.indexes.json`(31개)에 없는 equality+orderBy 쿼리 다수. 운영 확정 파손: `/api/calendar/public.ics`(status in+orderBy date) → **현재 프로덕션 500**. 동일 클래스: `me.ics`(attendeeIds+status+date), `me/export`(notifications userId+createdAt — 실패 시 개인정보 내보내기 전체 500), `bkend.ts` notifications(알림벨)·direct_messages(쪽지함)·diagnostic_results(진단 이력→아카이브 약점 추천 조용히 영구 비활성)·site_popups(listActive)·user_activity_logs·host_retrospectives. 콘솔 수동 인덱스로 일부만 동작 중인 상태 — 파일 기준 재배포 시 일괄 소실 위험. 수정: 실사용 쿼리 인덱스를 firestore.indexes.json에 정식 등재 + 누락분 생성.

## High

### 기능 파손
6. **회원 QR 출석 토큰이 어디서도 생성 안 됨** — `bkend.ts:501-504`, `seminar-store.ts:93`. qrToken 대입 0건 → 스태프 QR 스캔 체크인 항상 "등록되지 않은 QR", 회원 상세의 QR 표시도 undefined 렌더(크래시 가능성 PLAUSIBLE).
7. **발간 호수 상세가 비-staff에게 항상 빈 논문 목록** — `bkend.ts:3617-3626` vs research_journal_articles 룰. issueId 필터만으로 룰 증명 불가.
8. **협업 초대 수락 후에도 목록에 연구 안 보임** — `bkend.ts:3259-3290`. collaboratorIds denorm을 멤버가 못 갱신, reconcile은 리더 재방문 시에만.
9. **갤러리 사진 업로드 ~780KB 초과 원본 전부 실패** — `gallery/page.tsx:380-393`. base64 data URL 문서 저장(1MiB 한도) vs 5MB 클라이언트 게이트.
10. **비로그인 피드백 제출 항상 실패** — `feedback/page.tsx:74,220` vs `rules:1326`("익명 제출됩니다" 안내와 모순).
11. **외부활동 my-volunteer·review 비로그인 무한 로딩** — `activities/external/[id]/my-volunteer:45`, `review:70`.
12. **학회 후기 라운드업 비-staff 빈 화면** — `ConferenceRoundupView.tsx:54`(룰 증명 불가 쿼리).
13. **카드뉴스 콘솔: staff에게 편집 UI 노출되나 룰은 president+** — `console/layout.tsx:383` vs `rules:805-809`. 자동저장 무음(M)과 결합 시 편집분 유실.
14. ✔ **연구 채택률 KPI 4개 상시 0** — `api/console/adoption/route.ts:54-57`. Timestamp 컬렉션에 ISO 문자열 범위 비교(타입 브래키팅) → posts/comments/readingLogs/sessions 30d가 항상 0, 10월 KPI 판정 무의미화.
15. **연구 타이머 세션 수정 시 시각 -9h씩 파괴** — `EditSessionDialog.tsx:33-35`+`useStudySessions.ts:134-145`. UTC 저장값을 로컬로 재해석 저장 — 수정만 눌러도 세션 이동. "최근 세션" 목록도 UTC 표시.
16. **ResearchHero 페르소나 CTA 4종 dead anchor** — `ResearchHero.tsx:287-308`. `#계보`·`#키워드`·`#방법론` id 미존재 + 탭 내부라 스크롤 불가.
17. ✔ **플래시카드 복습 푸시 opt-in/opt-out 역전** — `push-admin.ts:78`+`MyPageView.tsx:1277`. 서버=미설정 발송, UI=미설정 OFF 표시 → 토글 꺼짐으로 보이는데 매일 푸시.

### 보안
18. **학회 개인 일정 PDF IDOR** — `api/conference/[programId]/my-schedule/pdf:15-19`. 무인증 userId 파라미터로 타인 일정 PDF 반환(룰 우회).
19. ✔ **회고 API 리더 판정 우회** — `api/activities/reflections/route.ts:51-62`. 쿼리는 progressId, 리더 판정은 요청 파라미터 activityId → 타 활동 회고 전문 열람. 판정을 문서의 activityId 기준으로.
20. **모임 회원 RSVP 정원 완전 우회** — `gatherings/page.tsx:215-232`. 게스트만 서버 정원 검사, 회원은 클라이언트 직접 write.
21. **대기자 승격 알림 스푸핑** — `api/seminars/waitlist-notify:15-66`. 일반 회원이 임의 문구 "참가 확정!" 인앱+푸시 발송 가능.

### UI 판독 불가(다크모드)
22. **/whats-new 카드 11장, 수업 일정 페이지, 저널 페이지 다크모드 판독 불가** — `whats-new/page.tsx:145-337`, `courses/[id]/schedule:989-1323`, `journal/*`. 파스텔 라이트 하드코딩 + foreground 텍스트.
23. **키워드 태그 대비 붕괴** — `ResearchHero.tsx:102,153`. amber·sky 그라디언트 위 흰 12px 글자 1.7~2.8:1.
24. ✔ **스테핑스톤 "로그인하기" 404** — `SemesterRoadmap.tsx:553` `/auth/login`(무존재) → `/login`.
25. **푸시 payload/토큰 위생 (PLAUSIBLE)** — `push-admin.ts`. webpush fcm_options.link 상대경로(절대 URL 필수) + invalid-argument를 만료 토큰과 동일 취급해 정상 토큰 삭제 위험.

## Medium (요약 — 상세는 각 항목 위치 참조)

- **모달 슬롯 경합** — `TodayTodosPopup.tsx:214` 외 3곳. 소유권 확인 없이 슬롯 null 덮어쓰기 → 팝업 2개 동시 노출 가능.
- **주간 다이제스트**: 미읽음 알림 줄이 정확히 타깃(이탈 회원)에게 발송 안 됨(`hasPersonalContent` 미검사, `weekly-digest:396-404`); 미읽음 5000건 초과 시 무작위 부분집계(:293).
- **알림 정리 크론 스캔 창 기아** — 30~90일 미읽음 1,500+건이면 창 독점(잔여, `notifications-cleanup:26-34`).
- **세미나 후기 요청 최대 4중 발송** — 당일(`seminar-store:127`)·전환(`seminar-status:74`)·D+1(`seminar-review-request:152`)·D+7 경로 간 교차 dedupe 없음, type도 `review_request`(유니온 외) vs `seminar_review_request` 불일치. 셀프 체크인은 당일 발송 누락(비대칭).
- **세미나 참석 토글 race** — 더블클릭 attendee 2건·정원 초과(`useSeminar.ts:165-193`); 운영진/연사 후기 중복 제출(`SeminarReviews.tsx:97`); 다중 연사 authorId 충돌(`speaker-review:338`).
- **게시글 수정이 투표 집계 롤백** — `useBoard.ts:214-217`(스냅샷 통째 재기록); 투표 제거 저장 안 됨(`if (data.poll)` truthy 가드).
- **이번 학기 계산이 방학(1~2·7~8월)에 붕괴** — `StudyTimerStats.tsx:17-24`·`MyGrowthWidget.tsx:38-42`: semStart가 미래 → 학기 통계 0. 지금(7월) 재현 중.
- **타이머 일시정지 시간이 duration에 합산** — `useStudySessions.ts:97-101`(DB startTime 기준 재계산) + timer30 보상 과지급.
- **탭 통합 후 딥링크 열화**: "연구 타이머"(`tab=report`)·"문헌 매트릭스"(`tab=reading`) 링크가 앵커 없이 탭 상단 착지(`ResearchJourneySection:76` 외); MyTodos "계획서/보고서" 할일·온보딩 CTA·whats-new 읽기 노트가 tab 파라미터 없이 기본 탭 착지(`MyTodosWidget:244-276`, `onboarding-next-cta:35-54`); AuthGuard returnUrl 쿼리 유실(`AuthGuard.tsx:29`) — 로그인 후 탭 컨텍스트 소실.
- **주제 탐색 잔결함**: 오염 answers로 frames 0개 빈 결과 카드(`teRecommend` 가드 부재)·localStorage "null" 크래시 (PLAUSIBLE, `TopicExplorer.tsx:55`).
- **readOnly 계약 위반(잠재)**: ResearchPaperList 삭제 버튼·다이얼로그 미가드(:238-248), readOnly 보기 다이얼로그 미마운트(:613), AlumniThesisRecommendations가 뷰어 프로필 변경(:718); PaperEditPage staff 열람 시 편집 폼+자동저장 반복 실패(:120-152).
- **연구 훅 undefined resolve** — `useResearchReport.ts:16` 외 2종: react-query v5 에러 처리 + ResearchJourneyGuide 동일 queryKey 이원화.
- **쪽지 단일 문서 양방 삭제** — `mypage/messages:140` + `rules:421-424`: 수신자 오클릭 한 번에 발신함에서도 소실, 확인 다이얼로그 없음.
- **스튜디오**: PPT 커버 골드 라인이 엠블럼 관통(`templates.ts:49-52`, 16:9만); 첫 저장 전 충돌 감지 통과(`StudioEditor.tsx:253`); 충돌 경로만 로컬 백업 누락(:254-259); cardnews 설명 160자 시 카드 이탈 (PLAUSIBLE).
- **콘솔**: 역할 변경 확인 없음+거짓 성공 토스트(`AdminMemberTab.tsx:373-378`); 게시글 통계 상시 0(`admin/analytics:89`, 카테고리 무필터 posts list 룰 거부); extraUsers 상시 빈 배열(`console/research/page.tsx:206`, `.data` 오참조); adoption -1 센티널 노출·에러 상태 부재.
- **권한/노출**: draft 학회보 데이터 레벨 노출(`rules:360` read if true); photos create 룰이 UI보다 넓음(인증만); /agents 게이트 불일치(admin 게이트 vs minRole staff); command palette 종합시험(auth)→staff 페이지 튕김; comm_boards 룰 광폭 (PLAUSIBLE).
- **팝업**: 편집 재저장마다 시작일 -1일(KST/UTC, `PopupEditor.tsx:45,64`); "session" dismiss 도달 불가; 로그인/재동의 화면 위 노출 (PLAUSIBLE).
- **협업**: 비멤버 하위 8페이지 무한 로딩; 멘션 인박스·내 마일스톤 쿼리 룰 증명 불가(미소비 훅 — 연결 즉시 실패).
- **기타**: AI 포럼 persona 키 미등록 크래시 (PLAUSIBLE); NetworkingPoll 게스트 7초 간격 permission-denied; SeminarLMS 권한 외 쿼리; 활동 주차 링크 어긋남·mutation onError 부재; MyActivitiesView attendeeIds 비배열 크래시 (PLAUSIBLE); NewMemberChecklist deps 누락으로 가짜 컨페티; MyGrowth 타일 2개가 공개 /research로 오연결; 게스트 RSVP TOCTOU+무효 rate limit; AI chat 익명 비용 남용 (PLAUSIBLE); 다크모드 하드코딩(research-analytics SVG·MultiAxisTrend 무효 CSS 변수·조교 리포트 칩); /archive/graph 노드색이 전역 타입색 규약과 상충; 정량vs정성 동일 hue 유사색; 키워드 클라우드 나선 반경 상한으로 60/80 옵션 취지 반감; 읽기 리스트 마지막 1건 제거 안 됨(undefined strip); 질적·혼합 연구방법 12종 published=false 시드 (PLAUSIBLE — 콘솔 발행 여부 확인 필요).

## Low (대표)

- `review_request` 타입 유니온 외 + 이중 수신 dedup 키 불일치; 내부 에러 문자열 노출 7곳; members/basic deny-list 투영(username 노출)·application-lookup 무 rate limit; 온보딩 evaluator 신규 3항목 케이스 누락(콘솔 매트릭스 0%); 개강 배너 alumni 노출; FloatingReadingTimer UTC 날짜; ReadingLogModal 상태 잔존(중복 기록); AdvisorFeedbackLog 배지 fallback 부재; 알림 설정 링크 `?tab=settings` 누락; 명함 탭 readOnly 빈 패널; help FAQ 마크다운 평문; steppingstone 무존재 라우트 3건(/analysis-notes 등); labs OG 필드 오참조·draft 직접 접근; 갤러리 photoCount/coverUrl 미갱신·고아 photos; card-news 결손 문서 크래시·ID 무검증 (PLAUSIBLE); LOGO 상수 미사용·lightFooter 텍스트 비잠금·스튜디오 readOnly 로컬 편집 허용; ResearchJourneyGuide legacy URL 발행(shim 의존)·"주제 탐색" 라벨 착지 불일치(tab=reading vs explore); 구 `tab=writing` 북마크 착지 변경; consent page 비표준 export (PLAUSIBLE); compexam 알림 무효 탭 파라미터; CSV undefined; CardNews 자동저장 무음.

## 이상 없음 확인 (중점 렌즈)

- **mirror 보상 원장**: 8개 기록 지점 멱등·소비처 제외 처리 전수 정합 — 이중 계산 없음. 스태프 대행 체크인 귀속 정상.
- **크론 인증**: 24개 라우트 전부 verifyCronAuth 정상, vercel.json 1:1 정합.
- **탭 개편**: 현행 `?tab=` 내부 링크 전수 유효, 구 URL shim 정상, 콘솔 readOnly 열람 영향 없음, tab=timer·"내 논문 작성" 잔존 없음.
- **스튜디오 로고**: 엠블럼이 편집 캔버스·PNG/PDF/PPTX 내보내기(html-to-image, 동일 오리진) 모두 정상 포함. locked 동작 일관.
- **투영 API**: HARD_SECRET 제거·화이트리스트 정상(members/basic deny-list 방식만 Low).
- **깨진 public 자산 없음**, 게스트 널 크래시 광범위 미발견(명시 건 제외).

## 권장 수정 순서

1. **운영 파손·데이터 손상 (즉시)**: #5 인덱스(public.ics 라이브 500), #1 인쇄, #2 훅 크래시, #15 세션 시각 파괴, #3·#4 협업(개강 전 협업 기능 채택 계획과 직결).
2. **보안**: #18 IDOR, #19 리더 우회, #20 정원 우회, #21 스푸핑.
3. **신뢰 훼손 UX**: #17 opt-in 역전, #6 QR, #14 KPI 0, 다크모드 묶음(#22-23), 탭 딥링크 묶음.
4. **Medium 클러스터 일괄** → Low는 후순위 배치.

---

## 처리 현황 (2026-07-05 완료)

- **Critical 5 / High 20 / Medium ~40 / Low ~25 전부 수정·배포 완료** (커밋 4건: Critical → High → Medium → Low).
- 조치 불요 확인: 질적·혼합 연구방법 12종 미발행(운영 DB 확인 결과 전부 발행 상태), CSV undefined(기존 가드 존재), comm_boards 룰(회원 개설이 의도된 설계로 판단).
- 보류 3건 운영 결정 반영 (2026-07-05):
  1. AI 챗봇 비로그인 — **차단 완료** (API 401 + 위젯 게스트 미노출).
  2. 다중 연사 후기 — **구조 변경 완료** (연사 선택 단계 + 연사별 authorId/1회 dedupe + PATCH 토큰 분기, 연사 수정 401 잠복 결함 동반 해결).
  3. 워크북 전체 후기 프리필 race — **수정 완료** (2026-07-06: 후기 로드 완료 후 폼 마운트 + updatedAt 키 동기화).
- 인프라: 복합 인덱스 10종 생성·firestore.indexes.json 41개로 정본화(reconcile 스크립트), 룰 3회 배포(협업 create/denorm·카드뉴스·피드백·학회보·photos·멘션/마일스톤).
