# OG·메타·alt 잔여 정정 R2 실행 보고 (v11-M5 · 2026-07-20)

> v10-M6(18라우트 정정) 이후 신설 라우트 커버리지 실측 및 정정 결과.
> 감사 기준: `git diff --diff-filter=A 8fc011ab..HEAD -- "src/app/**"` (v10 이후 신설 라우트)

---

## 감사 범위 및 결과

### 신설 공개 라우트

| 라우트 | 파일 | metadata 상태 | 처리 |
|---|---|---|---|
| `/activities/internal` | `src/app/activities/internal/page.tsx` | ✓ `export const metadata = { title: "대내 학술대회", description: "..." }` (server component) | 정정 불필요 |

### 신설 콘솔 라우트

| 라우트 | 파일 | metadata 상태 | 처리 |
|---|---|---|---|
| `/console/org` | `src/app/console/org/page.tsx` | ✗ 'use client', layout.tsx 없음, 루트 `robots: index: true` 상속 | **noindex layout.tsx 신설** |
| `/console/settings/org-chart` | `src/app/console/settings/org-chart/page.tsx` | redirect only (`redirect("/console/org")`) | 정정 불필요 (redirect) |

### 기존 미정정 라우트 (v10-M6 범위 외 · 정정 불필요 사유)

| 라우트 | 이유 |
|---|---|
| `/mypage/research/tools` | 'use client', 상위 `mypage/layout.tsx`가 `robots: { index: false, follow: false }` 적용 — noindex 정상 |
| `/archive/apa-style`, `/archive/citation-guide`, `/archive/literature-review-guide` 등 14개 archive 서브라우트 | 'use client', `archive/layout.tsx`의 "교육공학 아카이브" 제목 상속 — v10-M6 의도적 범위 외, v11 M5 신설 대상 아님 |

---

## 정정 내역

### 신설 1건: `/console/org/layout.tsx`

- **파일**: `src/app/console/org/layout.tsx` (신규 생성)
- **커밋 대상**: 3048736e에서 신설된 운영진 설정 콘솔 페이지
- **변경 내용**:
  ```typescript
  export const metadata: Metadata = {
    title: "운영진 설정",
    description: "연세교육공학회 운영진 구성 및 조직도를 관리하는 콘솔 페이지입니다.",
    robots: { index: false, follow: false },
  };
  ```
- **효과**: 루트 `robots: index: true` 상속 해제 → 콘솔 페이지 noindex 정책 적용
- **참고 패턴**: `src/app/mypage/layout.tsx` (동일 noindex 패턴)

---

## alt 정정

신규 라우트(`/activities/internal`, `/console/org`) 내 `alt=""` 의미 이미지 없음 — 정정 불필요.

---

## 콘솔 noindex 정책 현황

| 콘솔 sub-route | noindex 적용 방식 |
|---|---|
| `/console/org` | **이번 R2에서 layout.tsx 신설** |
| 기타 콘솔 라우트 (hackathon, cron-logs 등) | AuthGuard redirect 의존 (코드레벨 noindex 미설정) — v11 범위 외 |

> `console/layout.tsx`가 'use client'이므로 전역 console noindex 설정 불가. 신설 라우트 단위로 layout.tsx 신설이 현재 패턴.

---

## 검증

- `npx tsc --noEmit`: 에러 0 (신설 파일 표준 패턴 사용)
- `npx eslint src --quiet`: 경고 없음
- 수정 금지 영역 (whats-new, admin, framer-motion) 미접촉 확인
- build·commit 금지 준수

---

## 정정 라우트 수

**신설 layout.tsx: 1건** (`/console/org`)
