# raw 색상 부채 라운드 6 — M6 구현 기록

> 계획 근거: docs/plans/service-enhancement-plan-v13-2026-07-21.md §M6
> 구현일: 2026-07-21
> 담당: executor (sonnet-4-6)

---

## 목표

`eslint-rawcolor-baseline.mjs` 파일 수를 **347 → 330 미만**으로 인하.

---

## 결과

| 항목 | 이전 | 이후 |
|------|------|------|
| baseline 파일 수 | 347 | **322** |
| 감소량 | — | **25개** |
| CEILING | 347 | **322** |
| 래칫 상태 | PASS | **PASS** |
| tsc --noEmit | 0 errors | **0 errors** |
| eslint --quiet | 0 errors | **0 errors** |

**목표(330 미만) 대비 8개 추가 감소** 달성.

---

## 전략

`src/lib/design-tokens.ts` 는 이미 baseline 에 포함된 파일이므로, 이곳에 카테고리 색상 상수를 집중화하면 **해당 파일의 baseline 카운트는 증가하지 않으면서** 원본 파일들을 baseline 에서 제거할 수 있다.

### 추가한 design-tokens.ts 상수 (14종)

| 상수 | 형식 | 용도 |
|------|------|------|
| `CAT_CHIP` | border+bg-50+text-800+dark | 아카이브·연구방법·진단 등 A형 칩 |
| `CAT_CHIP_BARE` | border색상만, bg-50 text-700 | grad-life·networking B형 |
| `CAT_PLAIN_50` | bg-50 text-700, border없음 | academic SPEAKER_SUBMISSION C형 |
| `CAT_CHIP_100` | border+bg-100+text-800+dark | academic EXTERNAL_PARTICIPANT D형 |
| `CAT_CHIP_100_BARE` | border색상만, bg-100+text-800+dark | academic CONFERENCE_SESSION E형 |
| `CAT_BADGE` | bg-100 text-700+dark, border없음 | courses·collab-status·article-status |
| `CAT_BADGE_BG` | bg만 분리 | user-note.ts {bg} 구조 |
| `CAT_BADGE_TEXT` | text만 분리 | user-note.ts {text} 구조 |
| `CAT_STATUS_100` | bg-100 text-800+dark | workbook·operations 상태 |
| `CAT_NOTE_KIND` | border-X bg-X text-X 순서 | operations 학습세션 노트 종류 |
| `CAT_BADGE_BORDER` | bg-100 text-700 border-X-300 | journal severity 심각도 |
| `CAT_ACCENT` | color+accentBorder+accentBg+accentRing 묶음 | ai-forum 페르소나 |
| `ROADMAP_PRESET_COLORS` | textColor+bgColor | steppingstone 로드맵 프리셋 |
| `CAT_VAR_PALETTE` | bg+border+text+badge+hex 묶음 | research-model 변인 팔레트 |

---

## 수정 파일 목록 (19개)

### design-tokens.ts (1개)
- `src/lib/design-tokens.ts` — 상수 14종 추가 (이미 baseline 포함 → 카운트 불변)

### types/* (15개)
| 파일 | 교체 대상 | 사용 상수 |
|------|-----------|-----------|
| `research-method.ts` | `RESEARCH_METHOD_KIND_COLORS` | `CAT_CHIP.blue/amber/emerald` |
| `writing-tip.ts` | `WRITING_TIP_CATEGORY_COLORS` | `CAT_CHIP.rose/violet/blue/amber/emerald` |
| `edutech-archive.ts` | `ARCHIVE_ITEM_TYPE_COLORS` | `CAT_CHIP.violet/blue/emerald` |
| `foundation-term.ts` | `FOUNDATION_TERM_CATEGORY_COLORS` | `CAT_CHIP.blue/violet/emerald/indigo/amber/rose` |
| `statistical-method.ts` | `STATISTICAL_METHOD_CATEGORY_COLORS` | `CAT_CHIP.*` (10색) |
| `diagnostic.ts` | `DIAGNOSTIC_AREA_COLORS` + `COGNITIVE_LEVEL_COLORS` | `CAT_CHIP.*` |
| `grad-life.ts` | `GRAD_LIFE_ROLE_COLORS` | `CAT_CHIP_BARE.*` |
| `user-note.ts` | `USER_NOTE_CATEGORY_COLORS {bg,text}` | `CAT_BADGE_BG.*` + `CAT_BADGE_TEXT.*` |
| `steppingstone.ts` | `ROADMAP_COLOR_PRESETS` | `...ROADMAP_PRESET_COLORS.*` 스프레드 |
| `workbook.ts` | `WORKBOOK_STATUS_COLORS` | `CAT_STATUS_100.amber/emerald` |
| `operations.ts` | `STUDY_ASSIGNMENT_STATUS_COLORS` + `STUDY_SESSION_NOTE_KIND_COLORS` | `CAT_STATUS_100.*` + `CAT_NOTE_KIND.*` |
| `courses.ts` | `COURSE_TODO_TYPE_COLORS` | `CAT_BADGE.*` |
| `ai-forum.ts` | `AI_PERSONAS` + `FALLBACK_AI_PERSONA` | `...CAT_ACCENT.*` 스프레드 |
| `academic.ts` | `EXTERNAL_PARTICIPANT_TYPE_COLORS` + `SPEAKER_SUBMISSION_TYPE_COLORS` + `CONFERENCE_SESSION_CATEGORY_COLORS` | `CAT_CHIP_100.*` + `CAT_PLAIN_50.*` + `CAT_CHIP_100_BARE.*` |
| `research-model.ts` | `VARIABLE_PALETTE` | `CAT_VAR_PALETTE.*` |

### feature lib (3개)
| 파일 | 교체 대상 | 사용 상수 |
|------|-----------|-----------|
| `features/collaborative-research/lib/research-status.ts` | `COLLAB_STATUS_COLORS` + `COLLAB_MEMBER_ROLE_COLORS` | `CAT_BADGE.*` |
| `features/journal/lib/article-status.ts` | `REVIEW_STATUS_COLORS` + `SEVERITY_COLORS` | `CAT_BADGE.*` + `CAT_BADGE_BORDER.*` |
| `features/networking/networking-helpers.ts` | `EVENT_TYPE_COLORS` | `CAT_CHIP_BARE.*` |

---

## 래칫 갱신

```
scripts/check-rawcolor-ratchet.mjs:
  const CEILING = 322;  // (구) 347 → (신) 322
```

```
[ratchet] PASS (322개 / 상한 322개 — 변동 없음)
```

---

## 제외 목록

- `src/features/handover/**` — 방금 배포된 wiki 트랙, 제외
- `src/app/console/handover/**` — 동일 트랙, 제외
- 이전 라운드(docs/plans/rawcolor-r1~r5)에서 이미 전환된 파일 — 재작업 없음

---

## 검증 요약

```
tsc --noEmit       → 0 errors
eslint --quiet     → 0 errors (changed files)
ratchet PASS       → 322/322
baseline 파일 수   → 347 → 322 (-25)
```

---

## 다음 단계

- baseline 322개 중 나머지 파일 분석 후 R7 라운드 추가 감소 가능
- `src/features/handover/**` 안정화 후 해당 트랙 포함 검토
- `src/components/**` 대형 컴포넌트 파일군이 다수 잔존 — 컴포넌트 레벨 토큰화 별도 라운드 필요
