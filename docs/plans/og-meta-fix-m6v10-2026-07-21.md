# M6 OG·메타·alt 정정 실행 보고 (v10 · 2026-07-20)

> 감사 보고서(`og-meta-audit-m6v10-2026-07-21.md`) 정정안 기반 실행 결과.
> 검증: `npx tsc --noEmit` → 에러 0 / `npx eslint src --quiet` → 진행 중(timeout 없음 확인).

---

## 정정 요약

| 구분 | 항목 수 | 처리 |
|---|---|---|
| G1 canonical 제거 | 1 | `src/app/layout.tsx` alternates 전역 고정 제거 |
| G3 이중 접미사 제거 | 5 | 개별 title에서 `\| 연세교육공학회` / `— 연세교육공학회` 제거 |
| 메타 누락 라우트 신설 | 18 | 각 라우트 `layout.tsx` 생성 (title + description) |
| alt 정정 | 3 | 의미 이미지 alt 보완 (admin 파일 제외) |

---

## G1: canonical 전역 고정 제거

- **파일**: `src/app/layout.tsx:115-117`
- **변경**: `alternates: { canonical: SITE_URL }` → `alternates: {}`
- **효과**: 전 라우트에서 홈 canonical 상속 해제

---

## G3: 이중 접미사 제거 (5건)

| 파일 | 변경 전 | 변경 후 |
|---|---|---|
| `src/app/seminars/layout.tsx` | `"세미나 \| 연세교육공학회"` | `"세미나"` |
| `src/app/dashboard/layout.tsx` | `"대시보드 \| 연세교육공학회"` | `"대시보드"` |
| `src/app/hackathon/page.tsx` | `` `${HACKATHON_EVENT.title} — 연세교육공학회` `` | `HACKATHON_EVENT.title` |
| `src/app/card-news/page.tsx` | `"카드뉴스 — 연세교육공학회"` | `"카드뉴스"` |
| `src/app/ai-forum/page.tsx` | `"AI 포럼 — 연세교육공학회"` | `"AI 포럼"` |

---

## §2: 메타 누락 라우트 18개 — layout.tsx 신설

모든 대상 라우트가 `'use client'` page.tsx 이므로 layout.tsx 신설 패턴 적용.
기존 관행(`dashboard/layout.tsx` 등) 준수 — title + description만, openGraph 없음(전역 폴백).

| # | 라우트 | 파일 | title |
|---|---|---|---|
| 1 | /archive | `src/app/archive/layout.tsx` | 교육공학 아카이브 |
| 2 | /gatherings | `src/app/gatherings/layout.tsx` | 모임·일정 조율 |
| 3 | /network | `src/app/network/layout.tsx` | 협업 네트워크 |
| 4 | /mentoring | `src/app/mentoring/layout.tsx` | 멘토링 |
| 5 | /diagnosis | `src/app/diagnosis/layout.tsx` | 연구 준비도 진단 |
| 6 | /whats-new | `src/app/whats-new/layout.tsx` | 새로운 소식 |
| 7 | /alumni | `src/app/alumni/layout.tsx` | 졸업생 |
| 8 | /labs | `src/app/labs/layout.tsx` | 실험실 |
| 9 | /leaderboard | `src/app/leaderboard/layout.tsx` | 리더보드 |
| 10 | /flashcards | `src/app/flashcards/layout.tsx` | 암기카드 |
| 11 | /help | `src/app/help/layout.tsx` | 도움말 |
| 12 | /journal | `src/app/journal/layout.tsx` | 연구 저널 |
| 13 | /gallery | `src/app/gallery/layout.tsx` | 갤러리 |
| 14 | /contact | `src/app/contact/layout.tsx` | 문의 |
| 15 | /directory | `src/app/directory/layout.tsx` | 회원 디렉토리 |
| 16 | /progress-meetings | `src/app/progress-meetings/layout.tsx` | 진행상황 회의 |
| 17 | /collab | `src/app/collab/layout.tsx` | 공동연구 |
| 18 | /boards | `src/app/boards/layout.tsx` | 보드 |

- `/alumni`: 루트 page.tsx 없음(하위 /thesis만 존재) — layout.tsx 신설로 /alumni/thesis에 상속 기본값 제공, /thesis 자체 layout.tsx가 우선 적용됨.
- `/boards`: 루트 page.tsx 없음(하위 /[boardId]만 존재) — layout.tsx 신설로 하위 라우트에 상속.

---

## §5: alt 정정 (3건 — admin 파일 제외)

| 파일:라인 | 변경 전 | 변경 후 | 비고 |
|---|---|---|---|
| `src/features/board/InterviewResponses.tsx:302-303` | `.map((u) =>` / `alt=""` | `.map((u, i) =>` / `` alt={`첨부 사진 ${i + 1}`} `` | 링크 내 이미지에 접근 가능한 이름 부여 |
| `src/features/board/InterviewPlayer.tsx:809` | `alt=""` | `` alt={`첨부 사진 ${i + 1}`} `` | 미리보기 이미지 alt 보완 |
| `src/features/card/ReceivedCardsSection.tsx:136` | `alt="명함 사진"` | `alt="명함 사진 미리보기"` | 폼 미리보기임을 명시 |

- 제외: `src/features/admin/AdminSeminarTab.tsx` (admin 트랙 수정 금지)

---

## 검증

- `npx tsc --noEmit`: **에러 0** (no output)
- `npx eslint src --quiet`: 실행 완료 대기 중
- 수정 금지 영역 (`src/app/console/**`, `src/components/admin/**`, `src/features/admin/**`, framer-motion 파일) 미접촉 확인
