# Codex 배포 범위 런타임 리스크 리뷰 (2026-07-08)

리뷰 범위는 요청된 `07c31181..347445c8`입니다. `git diff --name-only 07c31181..347445c8 | wc -l` 기준 실제 변경 파일은 277개였고, 요청문에 적힌 31개와 일치하지 않았습니다. 참고로 31개 파일 범위는 `047234d4..347445c8`와 일치합니다. 본 리뷰는 명시된 커밋 범위의 diff를 확인한 뒤, 요청된 5개 관점에 해당하는 31개 기능 파일과 관련 소비자(캘린더, 갤러리, 검색, 대시보드, 알림/ICS)를 정적 분석했습니다. 리뷰일은 2026-07-08입니다.

## 높음 (High)

- `firestore.rules:226` — `networking_events`가 `visibility: "private"`와 `shareToken`을 도입했지만 서버 룰은 여전히 전체 공개 read라서, 직접 SDK 쿼리로 비공개/미발행 행사 메타데이터와 토큰 필드를 읽을 수 있습니다. `filter[shareToken]` 접근도 토큰 전용 허용이 아니라 전체 공개 룰에 기대고 있습니다. (근거: `firestore.rules:227` `allow read: if true;`, `src/types/networking.ts:59` "private: 공개 목록...숨기고", `src/types/networking.ts:60` "firestore.rules read 는 공개(if true) 유지", `src/lib/bkend.ts:529` `listPublished`, `src/lib/bkend.ts:533` `getByToken`)
- `src/features/dashboard/DashboardCommandCenter.tsx:49` — 대시보드 요약은 `networkingEventsApi.listPublished()` 결과를 `visibility !== "private"`로 다시 거르지 않아, published private 행사의 개수와 임박 행사 제목이 인증 사용자 대시보드에 노출될 수 있습니다. (근거: `src/lib/bkend.ts:529` `filter[published]`만 사용, `src/features/dashboard/DashboardCommandCenter.tsx:74` `upcomingEvents`, `src/features/dashboard/DashboardCommandCenter.tsx:83` `nextEvent`, `src/features/dashboard/DashboardCommandCenter.tsx:89` `name: nextEvent.title`)
- `src/features/networking/NetworkingPoll.tsx:232` — 운영진 일정 확정 시 슬롯 시간을 ISO datetime으로 파싱하는데, 이벤트 편집 폼은 시간대 옵션에 `"오후", "저녁"` 같은 자유 텍스트를 허용합니다. 이런 슬롯을 확정하면 `new Date("YYYY-MM-DDT오후:00").toISOString()`에서 `RangeError: Invalid time value`가 발생해 확정 작업이 런타임에서 실패합니다. (근거: `src/features/networking/EventEditorForm.tsx:35` "또는 저녁, 오후", `src/features/networking/EventEditorForm.tsx:238` placeholder `"18:00, 19:00 또는 오후, 저녁"`, `src/features/networking/NetworkingPoll.tsx:230` `const [date, time] = slot.split("|")`, `src/features/networking/NetworkingPoll.tsx:232` `toISOString()`)
- `src/features/networking/NetworkingPoll.tsx:88` — 후보 날짜를 만드는 경로가 `YYYY-MM-DD`를 local midnight `Date`로 만든 뒤 UTC ISO 문자열을 잘라 쓰는 기존 유틸에 의존합니다. KST 환경에서는 `2026-07-08T00:00:00+09:00`이 UTC로 `2026-07-07`이 되어 후보 슬롯이 하루 앞당겨질 수 있습니다. (근거: `src/features/networking/NetworkingPoll.tsx:89` `buildCandidateSlots(...)`, `src/features/networking/networking-utils.ts:10` `new Date(\`${startDate}T00:00:00\`)`, `src/features/networking/networking-utils.ts:14` `d.toISOString().slice(0, 10)`)

## 중간 (Medium)

- `src/features/networking/EventEditorForm.tsx:147` — 기존 고정 일정 행사를 poll 모드로 바꾸는 경우 `form.startAt`이 남아 있으면 poll 저장 payload에도 `startAt`이 계속 들어갑니다. 이후 카드가 `schedulingMode === "poll" && !ev.startAt`일 때만 투표 UI를 보여주므로, poll 행사인데 투표 UI가 렌더되지 않을 수 있습니다. (근거: `src/features/networking/EventEditorForm.tsx:145` "startAt이 빈 문자열로 남음" 주석, `src/features/networking/EventEditorForm.tsx:147` `startAt: localToIso(form.startAt)`, `src/features/networking/GatheringEventCard.tsx:71` `const isPollPending = ev.schedulingMode === "poll" && !ev.startAt`)
- `src/features/networking/NetworkingPoll.tsx:170` — `viewMonth`가 `event.pollPeriodStart`에서 한 번만 초기화되고, `event`가 바뀐 뒤 동기화되는 effect가 없습니다. 콘솔에서 선택 행사를 바꾸거나 같은 컴포넌트 인스턴스가 다른 poll 기간을 받으면, 월 경계(`monthBounds`)와 실제 표시 월(`viewMonth`)이 어긋나 후보 날짜가 안 보일 수 있습니다. (근거: `src/features/networking/NetworkingPoll.tsx:163` `monthBounds`는 props 의존, `src/features/networking/NetworkingPoll.tsx:170` `useState(() => ...)`, `src/features/networking/NetworkingPoll.tsx:186` `buildMonthCells(viewMonth)`)
- `src/lib/bkend.ts:1956` — `graduation_requirements`/`graduation_progress`는 repo의 `firestore.rules`에는 허용 규칙이 추가되어 있으나, 라이브 Firestore rules 배포 여부는 repo만으로 확인할 수 없습니다. 미배포 상태라면 마이페이지는 기본값으로 조용히 폴백한 뒤 저장 시 permission-denied가 발생합니다. (근거: `firestore.rules:930` `match /graduation_requirements`, `firestore.rules:934` `match /graduation_progress`, `src/features/mypage/GraduationChecklistCard.tsx:43` `getDefault()`, `src/features/mypage/GraduationChecklistCard.tsx:111` `graduationProgressApi.upsert(...)`, `src/app/console/graduation/page.tsx:105` `upsertDefault(...)`)
- `src/app/api/cron/networking-reminder/route.ts:36` — 알림 cron은 `visibility`를 읽거나 필터하지 않고 `published !== false`만 확인해 private 행사 제목을 `/gatherings` 링크 알림으로 보낼 수 있습니다. 참석자 대상이라 공개 누출 범위는 제한적이지만, 비공개 행사는 `/gatherings` 목록에서 필터되므로 비운영진 수신자는 알림 링크로 실제 행사에 도달하지 못할 수 있습니다. (근거: `src/app/api/cron/networking-reminder/route.ts:36` 타입에 `visibility` 없음, `src/app/api/cron/networking-reminder/route.ts:43` `ev.published === false`만 제외, `src/app/api/cron/networking-reminder/route.ts:68` `link: "/gatherings"`, `src/app/api/cron/networking-reminder/route.ts:71` 메시지에 `ev.title`)
- `src/app/api/calendar/me.ics/route.ts:292` — 주석은 "비공개 행사는 제외"라고 하지만 실제 데이터 타입과 조건에 `visibility` 검사가 없습니다. private 행사에 참석 RSVP가 있고 `published !== false`이면 개인 ICS에 포함될 수 있습니다. 개인 캘린더라 공개 노출은 제한적이나, 공유된 ICS URL에서는 제목/장소가 새어 나갈 수 있습니다. (근거: `src/app/api/calendar/me.ics/route.ts:292` 타입에 `visibility` 없음, `src/app/api/calendar/me.ics/route.ts:301` 주석, `src/app/api/calendar/me.ics/route.ts:302` 조건은 `published === false`만 제외)

## 낮음 (Low)

- `src/app/console/networking/page.tsx:102` — 콘솔 좌측 목록은 poll 미확정 행사(`startAt === ""`)도 항상 `formatEventDate(ev.startAt)`로 표시합니다. 상세 헤더는 `isPollPending`을 처리하지만, 목록은 `NaN월 NaN일(undefined) NaN:NaN` 같은 잘못된 날짜 문자열을 렌더할 수 있습니다. (근거: `src/app/console/networking/page.tsx:45` `b.startAt.localeCompare(a.startAt)`, `src/app/console/networking/page.tsx:102` `formatEventDate(ev.startAt)`, `src/app/console/networking/page.tsx:220` 상세 영역만 `isPollPending` 분기)

## 점검했으나 이상 없음

- null/undefined 직접 역참조, 빈 배열/Map에 대한 즉시 throw 패턴은 diff 범위의 신규 코드에서 확인되지 않았습니다. 예: 비공개 링크 페이지는 `(res.data as NetworkingEvent[])[0] ?? null`로 빈 결과를 처리합니다 (`src/app/gatherings/p/[token]/page.tsx:35`).
- `flashcards`/`diagnostic_results` 읽기는 `GlobalSearch`와 `LearningEffectCard` 모두 `userId` 필터를 사용하고, 현재 repo rules와 인덱스도 이에 맞습니다. (근거: `src/components/layout/GlobalSearch.tsx:160`, `src/features/mypage/LearningEffectCard.tsx:27`, `src/lib/bkend.ts:2549`, `src/lib/bkend.ts:2940`, `firestore.rules:1189`, `firestore.rules:1202`, `firestore.indexes.json:376`)
- 공개-facing 모임 목록, 캘린더, 갤러리 연결 후보는 클라이언트 레벨에서 private 행사를 다시 필터합니다. 단, 위 High/Medium 항목처럼 shared helper를 쓰는 다른 소비자에는 누락이 있습니다. (근거: `src/app/gatherings/page.tsx:66`, `src/app/calendar/page.tsx:465`, `src/app/gallery/page.tsx:60`)
- hydration mismatch는 정적으로 확정되는 신규 사례를 찾지 못했습니다. `window`/`localStorage` 접근은 effect 또는 이벤트 핸들러 안에 있고, 확인된 dependency 문제는 위 `NetworkingPoll`의 `viewMonth` 동기화 누락으로 별도 기록했습니다. (근거: `src/components/layout/GlobalSearch.tsx:106`, `src/components/layout/GlobalSearch.tsx:119`, `src/app/calendar/page.tsx:251`)
