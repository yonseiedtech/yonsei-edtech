# v14 백로그 (2026-07-21)

> 작성: 수석 서비스 플래너 (코드·git 실측 기반 · 대화형 인터뷰 없음)
> 대상: yonsei-edtech (Next.js 16 + Firestore · LIVE https://yonsei-edtech.vercel.app)
> 실측 기준: 2026-07-21 · HEAD=`fecd9df1` · rawcolor ratchet PASS (322/322) · deadcode ratchet PASS (93/93)
> **임박 이벤트: 에듀테크 해커톤 2026-08-22 (D-32) · 2026 후기 개강 09-01 (D-42)**

---

## 0. 재제안 금지선

### 0-1. v6~v12 전량 (재제안 금지)
v6 H1~H6·M1·M2·M6·L1~L3 / v7 13종 / v8~v12 전항목 — v12 계획서 §0 커밋 해시 확정분 전부.

### 0-2. v13 완료 항목 (재제안 절대 금지)

| v13 항목 | 산출물 | 커밋 | 상태 |
|---|---|---|---|
| **H1** 해커톤 종단 감사 + `HackathonTeamView boardId` 버그 수정 | boardId prop 제거, 팀원 모집 중 섹션 정상화 | `fecd9df1` | LIVE |
| **H2** 해커톤 사후 파이프라인 | `memberIds string[]` 팀원 크레딧·자동적재 ownerId∪memberIds·수상→포트폴리오 1클릭·아카이브 딥링크 프리필 | `81efb594` | LIVE |
| **H3·C5** 신학기 유입 코드 보정 | `cohortKeyOf` 8월 가입→다음학기 코호트 보정(테스트 10케이스)·`effectiveSemesterStart` 3곳 공용화 | `81efb594` | LIVE |
| **H4** 시즌 체크리스트 공유화·자동 판정 확대 | localStorage→`site_settings` 공유·체크자·시각 기록·auto 판정 3종 추가 | `81efb594` | LIVE |
| **H5** 개강 정합 C급 마감 (C-1·C-3·C-6) | 배너·방학카드 관례일→실개강일 공용화·휴학자 분기·digest 개강 D-14 블록 | `81efb594` | LIVE |
| **M1** 빈 상태 라운드3 완결 | 회원 대면 19파일 25건 `EmptyState` 수렴 | `81efb594` | LIVE |
| **M2** 교수설계 마법사 작성자 추적 | `activity_participations __design__` upsert·자동적재 소스5 | `81efb594` | LIVE |
| **M5** knip 데드코드 ratchet | baseline 93·`lint:deadcode`·`check-deadcode-ratchet.mjs` | `81efb594` | LIVE |
| **M6** raw 색상 부채 라운드6 | `design-tokens.ts` 14상수·19파일 정화·ratchet 347→322 | `c17f105c` | LIVE |
| **L3** whats-new 단일소스 | `FEATURE_DATES` Record에서 `addedAt` 자동 생성·수동 배열 제거 | `fecd9df1` | LIVE |
| **L4** TODO 트리아지 | `diagnostic.ts` 3건 의도적 유지 확정 | `fecd9df1` | LIVE |
| *(지시)* 대내 학술대회 CRUD | `site_settings internal_conferences`·메뉴 순서 조정 | `81efb594` | LIVE |
| *(지시)* 업무수행철 wiki 통합 | 복수 직책 태그·검색·작성/수정 별도 페이지 | `17f680f1` | LIVE |

**위 항목 전부 재제안 금지. M3(cron 임계)·M4(web_vitals 목표선)·L1(단축키 힌트)만 미완 이월.**

---

## 1. v14 핵심 명제

> **v13으로 해커톤 인프라(종단 리허설·사후 파이프라인·팀원 크레딧)·개강 정합(cohort 보정·시즌 체크리스트 공유화·digest 블록)·이중 ratchet 게이트(rawcolor 322·deadcode 93)가 완성됐다. 그러나 D-32(8/22)를 실측하면 세 가지 갭이 있다. (a) 팀 형성 단절: 아이디어 게시글과 팀 신청(`hackathon_registrations`)이 분리돼 "아이디어 보고 → 팀 합류"가 최소 2화면 수동 왕복이고, (b) 참가자 실시간 소통 공백: `/hackathon` 허브에 D-day 카운트다운·pinned 공지 노출이 없어 진행 상황을 능동적으로 파악하기 어려우며, (c) 개강 D-42 온보딩 미실주행: v13-H3가 코드(cohort 보정·semesterStart 공용화) 완성이지만 테스트 계정 실주행이 0회라 승인 이메일·착지 URL·첫 활동 마찰이 미측정 상태다.** v14 각도를 **"연결 · 소통 · 실주행 (Connect · Broadcast · Rehearse)"** 으로 잡는다 — 준비한 인프라를 끝까지 이어 붙이고(아이디어→팀), 참가자와 실시간으로 소통하며(공지·현황), 개강 직전 신입 파이프를 실제로 걷는다.

### 1-1. v14 실측 갭 (2026-07-21)

| # | 렌즈 | 실측된 갭 | 근거 |
|---|---|---|---|
| ① | **팀 형성 단절** | 아이디어 게시글→팀 신청 직결 경로 없음. "팀원 구함"/"아이디어 제안" 게시글 구분 없이 혼재 | `src/features/hackathon/HackathonTeamView.tsx` 실측 · `hackathon_registrations` 단독 라우트 |
| ② | **참가자 소통 공백** | `/hackathon` 허브에 D-day 카운트다운·pinned 공지 없음. 운영진 공지가 `/console` 내부에만 존재 | `src/app/hackathon/page.tsx` 실측 · `HackathonPhaseTimeline.tsx` 날짜 정적 |
| ③ | **신입 파이프 미실주행** | v13-H3 코드 보정 완료, 테스트 계정 실주행 0회 — 승인 이메일·착지·첫 활동 마찰 미측정 | `docs/plans/h3-h5-semester-fix-v13-2026-07-21.md` 보정 산출물 vs 리허설 보고서 부재 |
| ④ | **cron/web_vitals 데이터 대기** | v13-M3·M4 이월. 8월 초 2개월치 데이터 도래 예정 → v14 기간 내 착수 가능 | v13 §3 대기 항목 |
| ⑤ | **ratchet 정체** | rawcolor 322/322·knip 93/93 — ratchet은 회귀 차단이지 감축 엔진이 아님 | `scripts/check-rawcolor-ratchet.mjs` `scripts/check-deadcode-ratchet.mjs` 실행 결과 |

---

## High (이번 라운드 必 구현)

**H1: 해커톤 아이디어→팀 매칭 UX — 게시글에서 팀 합류 숏컷 + 아이디어/팀원구함 필터 (갭① · D-32 직결)**
- 문제/근거: `/hackathon` 허브에서 아이디어를 보고 팀에 합류하려면 ① 게시글 작성자에게 개별 연락 → ② `/hackathon/register` 이동 → ③ 등록 폼 작성의 3단계가 필요. 등록 폼(`HackathonRegistrations.tsx`)과 팀 뷰(`HackathonTeamView.tsx`)가 개별 구축돼 아이디어 글 참조 링크가 없다. v13-H1 종단 리허설에서도 이 단절이 잔존 확인됨(팀뷰 버그 수정은 완료, 흐름 연결 미착수).
- 제안 구현: **(1) 게시글 태그** — `comm_boards` 게시글 작성 폼에 `hackathonTag: "idea" | "team-wanted" | null` 선택 라디오 추가(기본 null·하위호환). **(2) 팀뷰 백링크** — 팀 등록 시 `ideaPostId` 선택 필드 추가(보드 게시글 참조) → `HackathonTeamView` 팀 카드에 "원본 아이디어 보기" 링크. **(3) 합류 신청 숏컷** — 아이디어/team-wanted 게시글 하단에 "이 팀 합류 신청" 버튼 → `/hackathon/register?teamName=XXX` 쿼리 프리필로 마찰 최소화. **(4) 허브 필터 탭** — `/hackathon` 게시판 영역에 "전체 / 아이디어 / 팀원 구함" chip 탭(클라이언트 필터, 서버 쿼리 추가 불필요).
- 담당 컴포넌트/파일: `src/features/hackathon/HackathonTeamView.tsx` · `src/app/hackathon/register/page.tsx` · `src/app/hackathon/page.tsx` · boards 게시글 작성 폼(`src/app/(community)/boards/`)
- 규모: **M** | 신규 컬렉션: **불필요** (`comm_boards` 필드 추가 + 등록 폼 쿼리 파라미터) | 리스크: 기존 게시글 `hackathonTag` 미설정 → null 처리로 하위호환. 팀명 프리필과 실제 등록 중복 가능 → 안내 문구로 완화.

---

**H2: 해커톤 참가자 실시간 소통 허브 — D-day 카운트·참가 현황·pinned 공지 (갭② · D-32 직결)**
- 문제/근거: `/hackathon` 허브가 단계(타임라인·팀뷰·수상 갤러리)를 보여주지만 **"지금 몇 팀이 참가 중인지", "마감까지 며칠인지", "최신 운영진 공지가 무엇인지"를 알려주는 실시간 표면이 없다.** `HackathonPhaseTimeline`의 날짜가 정적 텍스트뿐이며 D-day 역산이 없음. 운영진 공지는 `comm_boards` hackathon 컨텍스트에 있지만 허브 상단에 노출되지 않아 참가자가 `/boards`로 직접 이동해야 발견 가능.
- 제안 구현: **(1) D-day 배너** — 현재 phase에 맞는 마감일(접수 마감·제출 마감·수상 발표) 역산 카운트다운(`resolveHackathonPhase` + `HACKATHON_PHASE_TIMELINE[*].startDate` 기존 데이터 재사용). **(2) 참가 현황 카운터** — `hackathon_registrations` count를 허브 상단에 "N팀 참가 중" 뱃지(read-only, 비로그인 포함, Firestore `count()` aggregate로 비용 최소화). **(3) pinned 공지 인라인 표시** — `comm_boards` hackathon 컨텍스트에서 `pinned: true` 최신 공지 1건을 허브 상단 인라인 표시(기존 `boards` API 재사용, 운영진이 pin 설정 → 자동 노출). **(4) 제출 마감 D-day 강조** — submission 단계에서 "제출 마감 D-N" 배지를 amber(N≤7)/red(N≤3)로 단계별 강조.
- 담당 파일: `src/app/hackathon/page.tsx` · `src/features/hackathon/HackathonPhaseTimeline.tsx` · `src/lib/bkend.ts` (comm_boards pinned 쿼리 추가)
- 규모: **S~M** | 신규 컬렉션: **불필요** | 리스크: 실시간 count 비용 → Firestore `count()` aggregate 사용. pinned 공지 없을 때 빈 상태 처리 필수.

---

**H3: cron 임계 경보 게이트 — 해커톤 cron 안정성 자동 감시 (v13-M3 이월 · 8월 초 착수 · D-32 전 필수)**
- 문제/근거: v12-H2 cron 추세 리더가 LIVE로 성공률 스파크라인을 보여주지만, **임계 미달 시 자동 경보가 없어 운영진이 매번 직접 들여다봐야** 한다. D-32 앞 8월은 해커톤 관련 cron(`hackathon-submission-reminder`·`newcomer-activation-sequence`·`pending-signup-nudge`)이 최대 부하를 받는 시기. cron 하나가 조용히 실패해도 감지가 늦으면 참가자·신입이 알림을 못 받는다.
- 제안 구현: 8월 초 `api/console/cron-runs/trend` 실데이터(2개월 누적)로 kind별 성공률 분포 확인 → 임계 산정(예: 14일 이동 평균 < 80% && 직전 7일 대비 -15%p 동시 충족) → 하회 kind 발생 시 콘솔 랜딩 "cron 이상" 배지(기존 pending 큐 배지 v11-H3 패턴 재사용). `hackathon-submission-reminder` kind는 임계를 90%로 더 엄격히 설정. 경보 발송(push/email)은 §4 외부 의존 — 이번 라운드는 콘솔 내 배지·배너 표식까지.
- 담당 파일: `src/app/api/console/cron-runs/trend/route.ts` · `src/app/console/page.tsx` (pending 큐 배지 패턴) · `src/features/insights/CronLogsSection.tsx`
- 착수 시점: **8월 1~5일 (데이터 도래 확인 후 즉시)** — v14 기간 내 착수 확정
- 규모: **S** | 신규 컬렉션: **불필요** | 리스크: 일시 실패(서버 재시작 등) 오경보 → 14일 윈도 + 7일 비교 이중 조건으로 노이즈 억제. 초기엔 배지만(발송 없음).

---

**H4: 9/1 개강 신입 온보딩 종단 리허설 + 발견 결함 핫픽스 (갭③ · D-42 직결)**
- 문제/근거: v13-H3에서 `cohortKeyOf` 8월 가입 보정·`effectiveSemesterStart` 공용화·휴학자 분기가 완성됐으나, **테스트 계정으로 가입→승인→첫 로그인→첫 활동을 이어 걸어본 이력이 없다.** 코드 수정은 됐지만 승인 이메일 문구·착지 URL·`NewcomerProgressWidget` 표시 타이밍·스터디 신청 진입 마찰은 실측된 게 아니다. 개강 2주 전(8/18)까지 실주행해야 교정 여유가 있다.
- 제안 구현: **(1) 리허설 실행** — 테스트 계정으로 가입 4단계(약관→계정→학적→선택)→운영진 콘솔 승인→승인 이메일 수신·착지 URL 검증→첫 로그인(`NewcomerProgressWidget` 표시 여부·온보딩 배너)→첫 활동(진단평가 시작 또는 스터디 신청 1건). **(2) 체크리스트 기록** — 단계별 [기대 vs 실제] 표를 `docs/plans/newcomer-rehearsal-h4v14-{date}.md`로 기록. **(3) 발견 결함 경량 핫픽스** — 문구 오타·CTA 착지 오류·빈 상태 누락 등 표현 계층만 수정(로직 재설계 금지). 테스트 계정은 리허설 후 `delete-orphan-auth.mjs`로 삭제.
- 담당 파일: `src/app/(auth)/signup/` · `src/app/api/email/approval/route.ts` · `src/features/dashboard/NewcomerProgressWidget.tsx` · `src/app/api/cron/newcomer-activation-sequence/` · `src/lib/semester.ts`
- 착수 시점: **8월 18일 전** (개강 2주 전 여유 확보)
- 규모: **S** (리허설 자체) + 발견 결함 규모에 따라 S~M 추가 | 신규 컬렉션: **불필요** | 리스크: 프로덕션에서 테스트 계정이 알림 수신자 노출 → 테스트 전용 이메일 또는 오퍼레이터가 직접 수신(기존 패턴).

---

**H5: 개강 준비 콘텐츠 발행 딥링크 — 시즌 체크리스트 항목에서 실행 바로가기 (D-42 직결)**
- 문제/근거: v13-H4로 시즌 체크리스트가 공유화됐지만, 수동 항목(환영 공지 게시·아이디어 보드 공지·온보딩 시퀀스 확인 등)을 클릭해도 해당 화면으로 이동이 안 된다 — 운영진이 항목 확인 후 별도 메뉴로 수동 이동해야 한다. 콘솔 메뉴가 다수 분산돼 있어 개강 당일 혼선이 생길 수 있다.
- 제안 구현: `UpcomingSeasonCard` `SeasonItem`의 수동 체크 항목에 `href` 딥링크 추가 — ① "아이디어 보드 공지 게시" → `/boards/new?context=hackathon` 프리필 ② "환영 게시글 작성" → `/boards/new?context=main` ③ "온보딩 시퀀스 확인" → `/console` cron-runs 필터(기존 콘솔 URL 파라미터) ④ "학사정보 캠페인 확인" → `/console/settings/academic-status`. 딥링크가 없는 항목은 메뉴 경로 안내 텍스트. 기존 공유 저장(H4v13)·자동 판정 구조는 전혀 변경하지 않음.
- 담당 파일: `src/app/console/page.tsx` (`UpcomingSeasonCard` · `SeasonItem`)
- 규모: **S** | 신규 컬렉션: **불필요** | 리스크: boards/new 쿼리 파라미터 미지원 시 착지 후 빈 폼 → 미지원 페이지는 링크 없이 텍스트 안내로 대체.

---

## Medium (가능하면 구현)

**M1: web_vitals 라우트 목표선 · 회귀 표식 (v13-M4 이월 · 8월 초 착수)**
- 문제/근거: `web_vitals` 10% 샘플 v9-H6부터 축적, `WebVitalsSection`·`admin/insights`가 LIVE이나 목표선·회귀 감지 없이 수치 나열. 8월 초 = 2개월 누적 → 라우트별 p75 기준선 산정 가능.
- 제안 구현: 라우트별 p75 실데이터 확인 → `WebVitalsSection`에 Core Web Vitals 권장치(LCP 2.5s·CLS 0.1·INP 200ms) 대비 상태색 + 직전 2주 대비 악화 라우트 배지. 표본 N 미달 라우트는 "표본 부족" 레이블.
- 담당 파일: `src/features/insights/WebVitalsSection.tsx` · `src/app/admin/insights/page.tsx`
- 착수 시점: **8월 1~5일** (데이터 도래 후 H3와 동시 착수 가능 · 파일 영역 독립)
- 규모: **S** | 신규 컬렉션: **불필요** | 리스크: 저트래픽 라우트 노이즈 → N 미달 표시 완화.

---

**M2: kudos 리더보드 공개 — 해커톤·개강 참여 모멘텀 (9월초 볼륨 확인 후)**
- 문제/근거: v11-H2에서 kudos 관계 확장 완료. 해커톤·개강으로 kudos 볼륨이 최고조 예정이지만 **학회 전체 기여 순위·주차 추이가 공개 표면에 없어** 상호 인정의 사회적 압력이 작동하지 않는다.
- 제안 구현: `/dashboard` 또는 `/mypage`에 "이번 달 kudos TOP 5" 위젯 + 본인 순위 강조. 주차 집계(`kudos.created_at` 7일 버킷). 이름은 기존 `display_name` 사용.
- 담당 파일: `src/app/dashboard/page.tsx` · `src/features/kudos/` · `src/lib/bkend.ts`
- 착수 조건: **9월 1일 이후 kudos 누적 N≥50 확인 후** | 규모: **S~M** | 신규 컬렉션: **불필요** | 리스크: 볼륨 부족 시 리더보드 무의미 → 착수 전 count 확인.

---

**M3: 해커톤 수상 발표 운영 체크리스트 — 8/22 이후 즉시 실행 (D-32+)**
- 문제/근거: v13-H2에서 수상→포트폴리오 1클릭·아카이브 딥링크 프리필이 완성됐으나 **수상 발표 당일 운영진이 어떤 순서로 ① awards override 공개 ② 일괄 공개 ③ 포트폴리오 자동적재 안내 ④ 아카이브 등록 딥링크 안내를 수행하는지 운영 가이드가 없다.** 8/22 당일 혼란 예방이 목적.
- 제안 구현: **(1) 수상 발표 단계 체크리스트** — `UpcomingSeasonCard` awards phase에 전용 수동 항목 추가: "수상 등급 입력 완료" → `/console/hackathon 심사 탭` 딥링크 / "수상작 일괄 공개" → 일괄 공개 버튼 직접 클릭 안내 / "포트폴리오 자동적재 공지" → `/boards/new` 딥링크. **(2) 운영 가이드 인라인 텍스트** — 콘솔 hackathon 심사 탭 상단에 "수상 발표 절차" 접기 패널. 기존 코드 재사용, 추가 구현은 체크리스트 항목 + 안내 텍스트에 그침.
- 담당 파일: `src/app/console/page.tsx` · `src/app/console/hackathon/page.tsx`
- 착수 시점: **8월 초 (H1·H2 이후)** | 규모: **S** | 신규 컬렉션: **불필요** | 리스크: 없음(표현·안내 계층).

---

**M4: raw color 라운드7 — ratchet 상한 실질 인하 (322→<290)**
- 문제/근거: ratchet PASS(322/322)로 회귀는 막혔으나 감축이 v13(347→322) 이후 2라운드 정체. 남은 322개는 다크모드·브랜드 정합 잠재 결함원.
- 제안 구현: 회원 대면 고빈도 미처리 영역(게시판·gatherings·마이페이지 잔여·알림) raw 색상 → 시맨틱 토큰 전환(목표 <290). 완료 후 `gen-rawcolor-baseline.mjs` 재생성으로 상한 인하 고정.
- 담당 파일: `src/app/(community)/` · `src/app/gatherings/` · `src/app/mypage/` · `scripts/check-rawcolor-ratchet.mjs` · `src/lib/design-tokens.ts`
- 규모: **M** | 신규 컬렉션: **불필요** | 리스크: 다크모드 시각 회귀 → 전환 후 라이트/다크 스모크 필수.

---

**M5: knip unused exports 감축 — ratchet 상한 실질 인하 (93→<60)**
- 문제/근거: v13-M5에서 ratchet baseline 93 고정(회귀 차단). 이번 라운드가 첫 실질 감축 시작.
- 제안 구현: `npm run lint:deadcode` 결과에서 파일 2건(즉시 삭제)·export 82건 중 안전 삭제 상위 20~30건(미참조 타입·유틸 함수) 제거 → ratchet 상한 인하. ts-prune 오탐(동적 import·타입 전용) 허용 목록 정교화.
- 담당 파일: `scripts/check-deadcode-ratchet.mjs` · `knip.json` · 삭제 대상 다수
- 규모: **S~M** | 신규 컬렉션: **불필요** | 리스크: 동적 import 대상 삭제 시 런타임 오류 → 삭제 전 grep 교차 확인 필수.

---

**M6: 해커톤 실전 데이터 회고 집계 뷰 준비 (8/22 이후 · 대기)**
- 문제/근거: v13 §3에서 "8월 말 신규 편성 — 행사 후 실데이터로 v14 회고" 예고. 행사 전에 집계 쿼리만 준비해두면 당일 즉시 분석 가능.
- 제안 구현: `/console/hackathon` 심사 탭에 "행사 결과 요약" 섹션(참가 신청 N건·팀 확정 N팀·제출 N건·심사율·수상 N건·포트폴리오 자동적재 N건). 모두 기존 컬렉션 집계(신규 쿼리만 추가). 8/22 이후 데이터 채워지면 자동 완성.
- 담당 파일: `src/app/console/hackathon/page.tsx` · `src/lib/bkend.ts`
- 착수: **8/15 이후** (제출 데이터 누적 후) | 규모: **S** | 신규 컬렉션: **불필요** | 리스크: 없음(읽기 전용 집계).

---

## Low (여유 시 검토)

**L1: 단축키 상시 힌트 — Cmd+K 및 핵심 액션 tooltip 병기 (7라운드 이월 · 최종 경고)**
- Cmd+K 커맨드 팔레트 已구현. 핵심 버튼(저장·제출·이동)에 `<kbd>` tooltip 병기.
- 담당 파일: `src/components/ui/command-palette.tsx` · 각 핵심 버튼 컴포넌트
- 규모: **S** | 리스크: 없음.
- **경고: v14에서도 미구현 시 백로그에서 영구 삭제 검토 (v8부터 7라운드 이월).**

**L2: funnel 전환 개선 실험 (9/15 이후 신입 유입 데이터)**
- 개강 후 신입 N≥30 유입 시 funnel 병목 라우트 특정 → 개선 가설 1건 착수.
- 착수 조건: 9/15 이후 | 규모: **M** (데이터 도래 후 재평가)

**L3: loyalty/adoption 코호트 기준선 (10월 이후)**
- `adoption/loyalty-snapshot` cron 1회전(약 3개월) 후 코호트별 retention 기준선 산정.
- 착수 조건: 10월 이후 | 규모: **S** (분석) + **M** (개선 구현)

---

## 외부 의존 (운영진 결정 필요)

| 항목 | 의존 대상 | 코드 연결 |
|---|---|---|
| **X1: 해커톤 심사위원 배정 + 외부 심사위원 여부 결정** | 운영진 이벤트 기획 | 외부 심사위원 필요 시 `/hackathon/judge` 전용 라우트(judge role·Firestore rules 카브아웃) 구현 — S~M 추가 작업. 내부 운영진만 심사 시 현재 `/console/hackathon` 심사 탭으로 충분 |
| **X2: 해커톤 심사 루브릭 확정** (항목·배점·가중치) | 운영진 기획 | 현재 `JudgingCard`의 `scores` 자유 객체 → 루브릭 확정 시 항목별 입력 UI 고정 가능 (S) |
| **X3: 해커톤 장소 최종 확정** | 운영진 이벤트 확정 | `config.ts:place` 1줄 변경으로 즉시 반영 |
| **X4: 신학기 콘텐츠 작성** (환영 공지·스터디 모집·세미나 일정·신입 환영 게시글) | 운영진 콘텐츠 발행 | H5 딥링크 착지 페이지에서 직접 작성 — 코드는 준비됨 |
| **X5: cron 임계 경보 발송 정책** (quiet-hours·빈도·수신자) | 운영진 알림 정책 합의 | H3는 콘솔 배지까지. 발송(push/email)은 정책 확정 후 추가 (S) |
| **X6: weekly-digest 수신 대상 확대** (비활성 회원 재활성화) | 운영진 발송 정책 결정 | 발송 대상 필터 조정 코드 S 규모 |
| **X7: 네이버 서치어드바이저 인증 코드** | 사용자(계정 소유자) 발급 | 코드 수신 즉시 반영 가능 (S) |
| **X8: Firestore 정기 export/백업 GCP 스케줄 승인** | GCP 프로젝트 설정 | 장기 carryover |

---

## 3. 데이터 축적 대기 항목 (v14 기간 내 전환 예정)

| 대기 항목 | 의존 데이터 | v13 판정 | v14 재평가 |
|---|---|---|---|
| **cron 임계 경보 (H3)** | trend reader 2개월 성공률 분포 | 8월초 도래 | **8/1~5 착수** — H3 배치 |
| **web_vitals 목표선 (M1)** | 라우트별 p75 2개월 누적 | 8월초 재평가 | **8/1~5 착수** — M1 배치 |
| **kudos 리더보드 (M2)** | 해커톤·개강 kudos 볼륨 N≥50 | 9월초 | **9/1 이후** M2 착수 |
| **해커톤 실전 회고 (M6)** | 8/22 행사 실데이터 | 8월 말 신규 편성 | **8/22 이후 즉시** M6 착수 |
| **funnel 전환 개선 (L2)** | 개강 후 신입 유입 funnel | 9월초 | **9/15 이후** |
| **loyalty/adoption 코호트 (L3)** | 학기 경계 1회전 | 가을 학기 중 | **10월 이후** |

---

## 4. 즉시 착수 Top 5 (병렬 편성안 · 파일 영역 비중복)

| 트랙 | 항목 | 파일 영역 | 착수 |
|---|---|---|---|
| **트랙 A** | **H1** 아이디어→팀 매칭 | `src/features/hackathon/HackathonTeamView.tsx` · `src/app/hackathon/` · boards 게시폼 | 즉시 |
| **트랙 B** | **H2** D-day 허브 | `src/app/hackathon/page.tsx` · `HackathonPhaseTimeline.tsx` | 트랙 A 완료 후 순차 (같은 hackathon 영역) |
| **트랙 C** | **H4** 온보딩 종단 리허설 | `src/lib/semester.ts` · signup · newcomer cron (읽기 중심) | 즉시 (트랙 A·B와 파일 독립) |
| **트랙 D** | **H5** 체크리스트 딥링크 | `src/app/console/page.tsx` `SeasonItem` 단독 | 즉시 (트랙 C와 파일 독립) |
| **트랙 E** | **H3** cron 임계 경보 | `src/app/console/page.tsx` (배지) · `cron-runs/trend` | 8/1~5 데이터 도래 확인 후 |

> **병렬 규칙:**
> - 트랙 A와 B는 `src/app/hackathon/` 파일 영역 겹침 → **A 완료 후 B 착수(순차).**
> - 트랙 C·D는 트랙 A·B와 파일 영역 독립 → **즉시 병렬 launch 가능.**
> - 트랙 E는 8/1~5 데이터 도래 신호 확인 후 launch.
> - M4·M5(색상·deadcode 감축)는 표현 계층 — 타 트랙 완료 후 또는 소규모 병렬 배치.
> - **배포 게이트: tsc·build·rawcolor ratchet(322)·deadcode ratchet(93) 전량 PASS + 배포 후 QA 스모크(해커톤 허브·콘솔 랜딩·마이페이지·신규 가입 폼) 필수.**

---

## 5. 기술 부채 현황 (2026-07-21 실측)

| 부채 유형 | 현재 | 상한 | v14 목표 | 비고 |
|---|---|---|---|---|
| **raw color** | 322개 | 322 (ratchet PASS) | <290 (M4) | 2라운드 정체 해소 목표 |
| **knip unused** | 93개 | 93 (ratchet PASS) | <60 (M5) | baseline 고정 후 첫 실질 감축 |
| **빈 상태(ad-hoc)** | 완결 (M1v13) | — | 완료 | 19파일 수렴 완료 |
| **TODO/FIXME** | ~37건 | — | — | L4(v13) 트리아지 완료·잔여 의도적 유지 |
| **단축키 힌트** | 미구현 | — | L1 | v8~v14 7라운드 이월 — v14 미달 시 삭제 검토 |

---

*파일: `docs/plans/v14-backlog-2026-07-21.md` | 생성: 2026-07-21 | 다음 재검토: 8/22 해커톤 실전 데이터 확보 후 v15 편성*
