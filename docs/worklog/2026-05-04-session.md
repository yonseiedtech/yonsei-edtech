# 2026-05-04 세션 작업 정리

## 한눈 요약
오늘 14건 commit + 14회 deploy. 크게 **두 흐름**:
1. **보안 1단계 핫픽스 (Sprint 69)** — DB·API 우회 차단·cron 안정화·관측 강화
2. **연구보고서 인터뷰 모드 고도화 (Sprint 70~76)** — 가시성·동기부여·일관성·UX·데이터 정리

기타 hotfix: visit-tracker 카운트 누락, /board 라우트 매핑, 캘린더 ㄴ자/404, NextActionBanner 여백.

---

## 1. 보안 1단계 핫픽스 — Sprint 69

| Commit | 내용 |
|---|---|
| `4ac99e31` | visit-tracker 비로그인→로그인 시 회원 uniqueVisitors 누락 픽스 (sessionStorage 게이트 per-visitorId 추적) |
| `643c9694` | Sprint 69 Phase B+C+D 일괄: API 인증 우회 차단·cron 안정화·관측·UX |
| `3c3f3795` | app/error.tsx 진단 정보 노출 (오류 상세 접이식, 운영자 디버깅용) |

### Phase B — 인증 우회 차단 (API)
- `/api/reviews` POST: `authorRole` 클라 지정 무시 + 토큰 인증 시 `authorId` 위장 차단
- `/api/auth/auto-approve`: 가입 후 60분 윈도우 (위조 학번 후속 자동승인 차단)
- `/api/auth/resolve-email`: rate-limit 5/min + 응답 마스킹
- `/api/auth/check-username`: rate-limit 10/min (학번 enumeration 차단)
- `/api/parse-excel`: member → staff (xlsx ReDoS 방어)
- `/api/certificates/pdf`: requireAuth(staff) + HTML 1.5MB 제한 (DoS·SSRF 차단)
- 호출자 4곳에 토큰 헤더 첨부 (RegistrationsTab/CertificateGenerator/admin certs/cert email)

### Phase C — cron 안정화
- newsletter-publisher: status='publishing' 락으로 중복 발송 방지 + chunked Promise.all
- seminar-status / seminar-reminder / seminar-review-request: "1건이라도 있으면 batch 전체 skip" → 사용자 단위 가드 (후속 attendee 누락 픽스)
- seminar-status 감사장: speakerUserId / speakers[].userId 우선 매칭 (동명이인 오발송 차단)
- lecture-review-todos: today만 → 최근 3일 룩백 (cron 1회 실패 시 영구 누락 방지)
- 자동 수료증 번호: `lib/cert-counter.ts` transaction 카운터로 race-free
- card-news/conference 3파일 KST 헬퍼 적용 (UTC drift 픽스, Sprint 47 누락분)

### Phase D — 관측·UX
- visit-tracker `catch{}` → console.warn (silent failure 진단)
- cron 9개 라우트: `lib/cron-auth.ts` timing-safe Bearer
- next.config.ts: HSTS / X-Frame-Options / Referrer-Policy / Permissions-Policy
- forgot-password/answer: rate-limit 5/30min + 응답 지연 1.5s
- firestore.rules: `counters/{docId}` 클라이언트 read/write 차단

### 보류 항목 (별도 sprint)
- C1 `users.get` 인증 강제 (QR 명함 영향 분석 필요)
- C3 interview_responses 게이트 (visibility 필드 마이그레이션)
- C4 daily_visits write 제한 (visit-tracker 정합성)
- C5 seminar_attendees read 인증 (비로그인 카운트 페이지 영향)
- securityAnswer salt + PBKDF2 마이그레이션

---

## 2. 인터뷰 모드 고도화 — Sprint 70~76

### Sprint 70d/e (`1304e53b`) — 텍스트 정리
- 영문 ID 용어 한글 병기 (Context Analysis 맥락 분석, Learning Context 학습 맥락 등)
- 학자명은 한글 번역 안 함 (Smith & Ragan 등)
- field-format / field-scope-context prompt 구체화
- Transfer Context: "획득한 능력은 어디에서 발휘·활용되나요?"
- Orienting Context: 학습자 동기 + 운영 외적 제약 두 측면
- `*텍스트*` 강조 마크다운 모두 제거 (AI 티 제거)
- 적합 차시 가이드에 "지도교수님과 상의" 안내
- outcome-priority-domain "1개만 선택" 안내

### Sprint 72 (`d720f17e`) — Slide UX 작은 개선 4종
- F1: bridge-task-theory "우선 영역" → "학습자에게 우선적으로 변화시키려는 영역"
- F2: theory-name 핵심 이론 chip 선택 UI (18개 대표 이론 프리셋, 8개 노출 + 더보기)
- F3: theory-integration 조건부 노출 (이론 카드 < 2개 자동 skip)
- F4: outcome-mager-abcd → A·B·C·D 4 필드 분리 + legacy 호환 + 인터뷰/Editor 양쪽 동기화

### Sprint 73 (`2dad2d87`) — Navigator + 이전답변
- `isSlideAnswered(slideId, form)` + `getSlideAnswerPreview` 헬퍼 신설 (25 real slide 매핑)
- PreviousAnswerCard — 이전 답변 카드 + "비어있음 CTA" (가운데 정렬·큰 버튼)
- SlideNavigator — 챕터별 dot grid + 미작성 색상 + 클릭 점프
- 진행률 바 아래 details 형태로 SlideNavigator 노출
- 슬라이드 10개에 references 메타 추가

### Sprint 74 (`2dad2d87` 묶음) — 챕터 호흡 + 시각화
- ChapterCelebration — bridge 슬라이드 5종에서 챕터 완료 축하 (체크 펄스 + dot 진행 + 다음 챕터 인트로)
- ResearchLogicMap 사각형 4꼭지점 시각화 (TL=field, TR=middle, BL=prior, BR=theory)
- SVG 4변+2대각선 연결, 채움 색상 분기 (emerald/amber/rose)
- 각 노드 클릭 시 해당 챕터 첫 미작성 슬라이드로 점프

### Sprint 75 — 데이터 구조 변경 (사용자 명시 승인)
- F7: field-audience(Q1) / field-scope-context(Q8) 슬라이드 비공개 (학습자/환경 정보 통합)
  - DB 데이터(fieldAudience/scopeContext)는 그대로 보존, UI 노출만 차단
- 75-3: field-scope-exclude(Q9) prompt/hint 구체화 (대상/매체/유형/기간 4축)
- F5 데이터 모델: `ResearchReport.taskSteps?: string[]` + `FormState.taskSteps` + auto-split 마이그레이션
- F5 UI (`c5636b69`): TaskStepsField 컴포넌트 신설 — 단계별 input + 추가/삭제/순서 변경
  - 인터뷰·Editor 양쪽 공유 컴포넌트, legacy taskDecompose 자동 split, readOnly 표시 모드
- `2fda5bf9` hotfix: field-scope-exclude hint 키 중복 제거 (Vercel SWC 빌드 거부 → 즉시 fix)

### Sprint 76 (`5f3a4653`) — 진행률·LogicMap 애니메이션·이탈 confirm
- 진행률 바: 작성(emerald) / 미작성(rose) 두 영역 + 현재 위치 흰색 마커
- Navigator 미작성 dot: 빨간색(rose-400) 으로 명확화
- Logic Map: 연결선 굵기·색상 강화 + framer-motion `strokeDashoffset` 애니메이션 (선이 그려지는 효과)
- 인터뷰 dirty 상태 종료 confirm (X 버튼) + beforeunload 이벤트
- NextActionBanner 상단 여백 mt-2 → pt-1

### Sprint 76 F6 (`24ae0b6c`) — AI 번역투 일괄 정리
- 인터뷰 슬라이드 prompt/hint 자연스러운 한국어로 정리
- learner-profile prompt: "누구를 가르치는 연구인가요?" → "이 연구의 학습자는 어떤 사람들인가요?"
- theory-integration prompt: "여러 이론을 함께 본다면 어떻게 통합할 수 있을까요?"
- prior-summary prompt: "선행연구의 전반적인 흐름을 한 문단으로 요약해 주세요"
- Editor STEPS hints 6개도 동일 톤으로 동기화 (Context Analysis 맥락 분석 등)

---

## 3. 기타 hotfix

### 캘린더 (`29f0ff41`)
- 이어지는 일정 바에 `↳` 텍스트 + CornerDownRight 아이콘이 동시 표시 → 텍스트 제거 (아이콘만 유지)
- 활동 클릭 시 404: 캘린더가 type 단수(`study`/`project`)로 라우트 보내는데 폴더는 복수(`studies`/`projects`)
  → ACTIVITY_ROUTE_PATH 매핑 추가
- NextActionBanner 상단 여백 pt-1 → 제거

### Board (`672a12ac`)
- **paper_review 첨부 논문 저장 누락 버그**: useCreatePost / useUpdatePost payload 빌더가 `linkedPaper` 필드를 빠뜨려 firestore에 저장 안 됨 → 둘 다 명시적 추가
- PostForm 투표 첨부 토글 (선택적 적용): `pollSectionOpen` state, "+ 투표 첨부 (선택)" 버튼 클릭 시만 노출
- /board/[id] "목록으로" routes 매핑에 paper_review → /board/paper-review 추가

### 카테고리 락 + 분석노트 라벨 (`08bf871a`)
- PostForm 수정 모드: 카테고리 chip disabled + "변경할 수 없습니다" 안내 (orphan 데이터 방지)
- LinkedPaperCard 버튼: "내 논문 읽기에 저장" → "내 분석 노트에 추가"
- 토스트 안내: "마이페이지 → 연구 → 논문 읽기 탭에서 확인"
- ResearchPaperList 섹션 타이틀: "연구 논문" → "분석 노트"

### Dashboard·Archive (`b8bd0523`)
- 대시보드 NextActionBanner와 PageHeader 사이 여백 mt-3 → mt-6 sm:mt-8
- /archive EduTechOverview에 AECT 미국 교육공학회 섹션 신설
  - AECT 2008 공식 정의 영문 인용 + 의역 + 6개 키워드 칩
  - 정의 변천 타임라인 (1963 → 2008)
  - 주요 학술지 (ETR&D, TechTrends), Divisions 6개, 핵심 이론 7개
  - 한국 교육공학회 (KSET)와의 관계 안내

---

## 다음 sprint 후보 (보류)

### 보안 2단계 (사용자 결정 대기)
- C1 `users.get` 인증 강제 — QR 명함 영향 분석 필요
- C3 interview_responses 게이트 — visibility 필드 마이그레이션
- C4 daily_visits write 제한 — visit-tracker 정합성
- C5 seminar_attendees read 인증 — 비로그인 카운트 페이지 영향
- securityAnswer salt + PBKDF2 마이그레이션 (legacy hash 자동 재해시)

### 인터뷰 모드 추가 아이디어
- 사용자가 직접 입력하는 다른 슬라이드의 list UI 확장 (outcome-cognitive 등)
- Logic Map 노드별 "지난 답변 살짝 미리보기" 호버
- 보고서 본문 출력 톤 일관성 점검

### 검증·운영
- /courses 에러는 사용자 측에서 해결됨
- 보안 1단계 + 인터뷰 모드 고도화의 실사용 피드백 수집 후 다음 우선순위 정리
