# 2026-05-23 세션 워크로그 — 학술대회 운영 + 아카이브 확장

## 세션 개요
- 배포 횟수: 12회 (`e30582aa` ~ `5cba4712`, 2026-05-22 17:21 ~ 2026-05-23 10:42 KST)
- 주요 영역:
  - 대외 학술대회 신청 흐름 안정화 (CSV 내보내기 · 폼 빌더 stale closure 픽스 · 비회원 본인 신청현황 조회)
  - 보안 강화 (신청자 PII를 별도 비공개 컬렉션으로 분리 · 신청현황 권한 정비 · 콘솔 비밀번호 초기화 방식 전환)
  - 잠재회원 흐름 Phase A/B (콘솔 집계 뷰 + 회원가입 시 게스트 이력 사전 안내)
  - 대외 학술대회 운영 도구 노출/필터 개선 (발표자 탭 유형 필터 · 운영진 항상 노출)
  - 마이페이지 공개 프로필 진입 버튼
  - 자원봉사자 운영 콘솔 고도화 + 발표자 운영 콘솔 Phase 1 신설
- 결과: 신청-운영-사후 흐름이 PII 분리 위에서 통합 운영 도구를 갖춘 상태로 정리됨. 1회성 마이그레이션 절차 필요 (운영자 작업 항목 참조).

## 배포된 기능 (커밋 SHA 순)

### 1. 신청현황 탭 CSV 내보내기 — `e30582aa`
- 배경: 운영자가 신청현황을 엑셀에서 가공할 채널이 없어 수기 복사로 처리.
- 구현 내용: 운영진 전용 신청현황 탭에 CSV 버튼 추가. 컬럼은 이름·학번·이메일·핸드폰번호(+참석유형)·상태·신청일 + 폼 질문별 답변. 현재 유형 필터 적용분만 가로형 1인 1행으로 내보내기. schedule/datetime_slots 답변은 "전체 시간 가능 / 참여 제한 / 슬롯 목록" 형태로 정규화. UTF-8 BOM 포함으로 엑셀 한글 깨짐 방지.
- 영향 파일/컬렉션: 신청현황 탭 컴포넌트.

### 2. 신청폼 빌더 복제 stale closure 픽스 — `dbfa7821`
- 배경: 시간 입력 수정 직후 복제 클릭하면 (blur→input→click 한 task 배치) 복제본 시작 시간 누락 + 원본까지 되돌림.
- 구현 내용: 모든 뮤테이터(duplicate/update/move/remove)가 render closure의 `fields` 대신 `workingRef.current`(최신 의도 상태)를 읽도록 변경. 렌더 타이밍/배치와 무관하게 정확한 값 위에서 동작.
- 영향 파일/컬렉션: form-builder 컴포넌트.

### 3. 대외활동 신청현황 조회 (비회원 본인 확인) — `27c664b7`
- 배경: 비회원 신청자가 본인 신청 상태를 확인할 경로가 없음.
- 구현 내용: "참가 신청" 옆에 "신청현황 조회" 버튼 추가 (대외활동 한정). 이름+학번으로 본인 신청 1건 조회 — 상태(대기/승인/거절)·참석유형·신청일·발표 제목·폼 답변 표시. 일치 항목 없으면 안내. 운영진 전용 신청현황/리포트 탭은 isStaff 게이트로 구조적으로 분리됨.
- 영향 파일/컬렉션: ActivityDetail 신청현황 조회 다이얼로그.

### 4. 신청현황 표시 권한 + 비번찾기/콘솔 비번 초기화 — `c07689e3`
- 배경: 비운영진에게 자원봉사자 명단 노출 + 발표자 승인 상태 노출 + 6자리 연도 입력 가능 + 비번 초기화가 이메일 발송 방식이라 비활성 계정 처리 곤란.
- 구현 내용:
  - 자원봉사자 탭: staff+ 전용으로 제한.
  - 발표자 탭: 목록은 공개 유지, 승인/대기/반려 상태는 운영진에게만 표시.
  - 비번 찾기 생년월일: `min`/`max` 추가로 4자리 연도 고정.
  - 콘솔 비밀번호 초기화: 이메일 발송 → 학번 입력 후 직접 초기화 방식 (신규 `POST /api/admin/reset-password`, confirm 단계 추가).
- 영향 파일/컬렉션: 발표자/자원봉사자 탭, 비번 찾기 폼, 운영 콘솔.

### 5. 신청자 PII 분리 — `041c87be` (가장 큰 보안 변경)
- 배경: 신청자 개인정보(이름·학번·이메일·전화·답변)가 공개 `activities/{id}` 문서에 임베드되어 read 권한 잡음.
- 구현 내용:
  - 신규 컬렉션 `activity_applicants` 도입 (staff 전용 read/write, firestore.rules 9줄 추가).
  - 신규 서버 API 6종: `POST /api/activities/[id]/apply` (신청·수정), `GET /my-application`, `POST /application-lookup`, `GET /me/applications`, `POST /auth/link-guest-applicants`, `POST /admin/migrate-applicants`.
  - `bkend.ts` 에 `activityApplicantsApi` (get/mutate 트랜잭션) 추가 + dual-read fallback 로 마이그레이션 전/후 무중단.
  - 비-staff 신청/수정/연동은 모두 서버 라우트(Admin SDK) 경유로 통일.
  - `activities` 문서에는 비-PII `publicSpeakers` 만 투영 (발표자 탭 공개용).
  - `application-lookup` 응답은 화이트리스트 투영(PII 미반환), `editKey` 수정은 인증 본인/staff 한정, 인증 회원의 학번·이메일은 프로필에서 취득.
  - 1회성 운영 콘솔 `/console/migrate-applicants` 페이지 (복사/검증/임베드 제거 2단계).
- 영향 파일/컬렉션: 19파일 / +1,420 −212. 신규 컬렉션 `activity_applicants`. `activities` 컬렉션 필드 스키마 변경 (PII 제거, `publicSpeakers` 추가).
- 품질 게이트: 배포 후 `/console/migrate-applicants` 에서 1단계(복사) → 검증 → 2단계(제거) 수동 실행 필요. dual-read fallback 으로 점진 마이그레이션 무중단.

### 6. 잠재회원 Phase A — 콘솔 뷰 + 모바일 팝업 정렬 — `61842f1a`
- 배경: 비회원으로 학술대회·세미나에 참여한 사람을 운영진이 한눈에 볼 수단 없음.
- 구현 내용:
  - `GET /api/console/potential-members` (staff 전용): `activity_applicants` + 세미나 게스트 등록 dual-read 후 학번 기준으로 집계, 이미 가입한 학번 제외.
  - `/console/potential-members` 페이지 신설 (검색 / 데스크톱 테이블 / 모바일 카드).
  - 콘솔 "회원" 네비에 "잠재회원" 항목 추가.
  - 회원가입 유도 팝업 모바일에서 DialogContent 전체 높이 점유 문제 → 내용 크기에 맞춰 중앙 정렬(`top-1/2 -translate-y-1/2 max-h`).
- 영향 파일/컬렉션: 신규 API + 신규 페이지 + 콘솔 layout 네비.

### 7. 잠재회원 Phase B — 회원가입 시 활동 이력 사전 안내 — `9f13b320`
- 배경: 비회원 이력 보유자가 가입 직전에 본인 이력을 확인할 수단 없음.
- 구현 내용:
  - `GET /api/auth/guest-history-preview` (인증 불필요, IP rate-limit 10/60s, 제목·날짜·종류만 반환 — PII 미노출).
  - `GuestHistoryPreviewDialog` 컴포넌트: 이력 발견 시 "가입 완료 시 자동 연동" 안내.
  - `SignupMultiStep`: Step 1 통과 후 비차단 조회, `count > 0` 시 팝업 (학번+이메일 키로 1회성, 조회 실패해도 가입 흐름 유지).
  - 실제 연동은 기존 `runAllGuestLinkers` 그대로 사용 — 사전 안내 UX만 추가.
- 영향 파일/컬렉션: 신규 API + signup 다이얼로그.

### 8. 발표자 탭 유형 필터 — `8a997673`
- 배경: 발표자 탭의 정적 카운트 배지가 필터로 동작하지 않음.
- 구현 내용: 전체/논문/포스터/미디어전 선택 시 해당 구분 발표자만 표시. 선택 시 ring 강조, 빈 결과 안내 추가.
- 영향 파일/컬렉션: ActivityDetail 발표자 탭.

### 9. 대외 학술대회 운영 도구 노출 조건 변경 — `8de9682c`
- 배경: 운영 도구 박스가 `backHref`(접속 경로)에 의존 → 관리자가 공개 URL로 직접 접속 시 운영 도구 미노출.
- 구현 내용: 대외 학술대회는 staff+이면 접속 경로와 무관하게 노출되도록 조건 변경.
- 영향 파일/컬렉션: ActivityDetail.

### 10. 마이페이지 공개 프로필 보기 버튼 — `0561b740`
- 배경: 마이페이지에서 본인의 공개 프로필(`/profile/[id]`)로 이동할 경로 없음.
- 구현 내용: 프로필 카드에 "공개 프로필 보기" 버튼 추가, `/profile/{user.id}?from=mypage` 로 이동. readOnly(관리자 미리보기) 모드에서는 미표시.
- 영향 파일/컬렉션: MyPageView.

### 11. 자원봉사자 운영 콘솔 고도화 — `1f4f6f1b`
- 배경: 읽기 전용 모니터링에서 운영 도구로 승격 필요.
- 구현 내용:
  - 신청자↔배정 통합 + 미배정자 인라인 배정 (`userId → guestKey → studentId` 우선순위 매칭, 동명이인 오매칭 방지, rejected 제외).
  - 시간대별 가능 인원 시간표 (`AvailabilityTimeGrid`): 신청폼 `schedule`/`datetime_slots` 답변 집계, `__ALL__`/`__RESTRICTED__` 마커 처리, 부족 슬롯 경고.
  - 인원별 체크리스트 배분 (`BulkDutyDialog`): duties 추가·삭제·체크 + 역할별 일괄 배분. `volunteerAssignmentsApi.mutateDuties` 트랜잭션으로 lost-update 방지, `Promise.allSettled` 로 부분 실패 집계, `crypto.randomUUID` id.
  - 배정 인라인 편집 (`AssignmentDialog`): 역할·시프트·비상연락처·메모 수정·삭제.
- 영향 파일/컬렉션: 8파일 / +1,547 −130. `VolunteerAssignment` 타입에 `userStudentId`·`guestKey` 비정규화 필드 추가. firestore.rules 의 기존 staff read/write 사용.

### 12. 발표자 운영 콘솔 Phase 1 — `5cba4712`
- 배경: 자원봉사자 콘솔 패턴을 발표자에게 미러링. 발표 유형별로 차별화된 준비 흐름이 필요.
- 구현 내용:
  - 신청자↔배정 통합 + 미배정자 인라인 배정 (`userId → guestKey → studentId` + `submissionType` 매칭, rejected 제외, 다중 유형 id 충돌 방지).
  - 유형별 표준 체크리스트 자동 시드 (paper 5 / poster 4 / media 4) + 일괄 배분 (`mutateTasks` 트랜잭션, `Promise.allSettled`).
  - 유형별 세부 정보 폼: paper(트랙/세션/좌장), poster(부스/위치), media(공간/장비/설치).
  - 인라인 편집·삭제: 유형 변경 시 `deleteField` 로 stale details 정리, `stripUndefinedDeep` 가 `FieldValue` 를 통과시키도록 가드.
  - 진입점: ActivityDetail 운영 도구 박스 "발표자 운영" 링크.
  - ESLint `set-state-in-effect` 제거 (`useEffect` → 동기 핸들러).
- 영향 파일/컬렉션: 10파일 / +2,085 −3. 신규 컬렉션 `speaker_assignments` (firestore.rules staff+). 신규 타입 `src/types/speaker.ts`.

## 데이터 모델 변경 사항
- 신규 컬렉션:
  - `activity_applicants` — 신청자 PII 비공개 문서 (staff 전용, `041c87be`).
  - `speaker_assignments` — 발표자 운영 콘솔 데이터 (staff 전용, `5cba4712`).
- 기존 타입 확장:
  - `VolunteerAssignment` — `userStudentId`·`guestKey` 비정규화 필드 추가 (`1f4f6f1b`).
  - `activities` 컬렉션 — PII 임베드 제거, 비-PII `publicSpeakers` 투영 추가 (`041c87be`).
  - `src/types/speaker.ts` 신규 (`5cba4712`).
- Firestore 규칙 변경:
  - `firestore.rules` 9줄 추가: `activity_applicants` staff 전용 read/write (`041c87be`).
  - `firestore.rules` 7줄 추가: `speaker_assignments` staff 전용 read/write (`5cba4712`).

## 운영자 운용 가이드 (간단)

### 신청자 PII 마이그레이션 절차
1. 배포 완료 후 `/console/migrate-applicants` 접속 (staff+).
2. **1단계 — 복사**: 기존 `activities/{id}` 임베드 신청자를 `activity_applicants` 로 복사. dual-read fallback 으로 무중단.
3. **검증**: 신청현황 탭/CSV/마이페이지 활동에서 데이터가 정상 표시되는지 확인.
4. **2단계 — 임베드 제거**: `activities/{id}` 문서에서 PII 필드 제거. 이후부터는 `activity_applicants` 가 단일 소스.
5. 신규 신청은 `POST /api/activities/[id]/apply` 가 자동으로 신규 컬렉션에 기록.

### 자원봉사자 운영 콘솔 (`/console/academic/external/[id]/volunteers`)
- 좌측: 자원봉사 신청자 목록. 미배정자는 카드에서 인라인 배정 가능.
- 중앙 시간표: 신청자가 선택한 가능 시간대를 집계. 부족 슬롯은 경고 색상.
- 우측/배정 카드: 역할·시프트·비상연락처·메모 인라인 편집. 카드 내 duties 체크리스트로 임무 부여, "일괄 배분" 버튼으로 역할별 한 번에 배분.

### 발표자 운영 콘솔 (`/console/academic/external/[id]/speakers`)
- 발표 유형별(paper/poster/media) 카드 분리. 카드에 유형별 세부 정보 폼 (paper: 트랙/세션/좌장, poster: 부스/위치, media: 공간/장비/설치).
- 미배정 신청자는 유형 매칭 후 인라인 배정. 다중 유형 신청 시 id 충돌 방지 로직 적용됨.
- "일괄 시드" 로 유형별 표준 체크리스트(paper 5/poster 4/media 4)를 한 번에 부여.
- 진입: ActivityDetail 운영 도구 박스 "발표자 운영" 링크.

## 진행 중 / 다음 작업
- 연구방법 가이드 Phase 1 (진행 중)
- 연구방법 가이드 Phase 1.5 — 통계방법 + 비교표 + 대안 추천 (큐)
- CSV 내보내기 클릭 무반응 진단 (사용자 콘솔 에러 대기 중)

## 알려진 follow-up (비차단)
- 자원봉사자/발표자 콘솔 리뷰에서 deferred 된 MEDIUM/LOW 항목 (별도 리뷰 노트 참조)
- `activity_applicants` 마이그레이션 미완 환경에서는 dual-read fallback 이 작동하지만, 모든 활동에 대해 마이그레이션 완료 시점에 fallback 코드 정리 검토
